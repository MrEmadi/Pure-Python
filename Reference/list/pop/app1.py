# list.pop(index):
# list -> 'list' data type
# index = -1 -> int
# return type -> T (removed item: str, int or ...)
# Empty list or index is out of range -> IndexError

names = ['Zahra', 'Amir', 'Bagher']

names.pop()
print(names)

names.pop(0)
print(names)
